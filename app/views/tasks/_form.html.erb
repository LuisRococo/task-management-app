<%= form_with model: [@task_list, @task] do |form| %>
    <div class="form-group mb-3">
        <%= form.label :title%>
        <%= form.text_field :title, class: 'form-control', placeholder: 'Enter a title' %>
    </div>

    <div class="form-group mb-3">
        <%= form.label :task_list_id%>
        <%= select_tag :task_list_id, options_from_collection_for_select(@task.task_list.board.task_lists, 'id', 'title'), class: 'form-control' %>
    </div>

    <div class="form-group mb-3">
        <%= form.label :content %>
        <%= form.rich_text_area :content %>
    </div>

    <div class="d-flex justify-content-end mt-5">
        <%= link_to 'Return', :back, class: 'btn btn-primary me-2' %>
        <%= form.submit class: 'btn btn-success' %>
    </div>
<% end %>
